on:
  pull_request:
    branches: [ 'master' ]
    types: [ opened ]

jobs:
  add_pr_label_job:
    runs-on: ubuntu-latest
    steps:
      - id: add-pr-label
        uses: sanjay-sridharan-personal/javascript@56bb06763744025d591feeced8d80aff9c6f191c
        with:
          GH_PAT: ${{ secrets.GH_PAT }}
