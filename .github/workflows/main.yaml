on:
  pull_request:
    branches: [ 'interview' ]
    types: [ opened ]

jobs:
  add_pr_label_job:
    runs-on: ubuntu-latest
    steps:
      - id: check-pr-title
        uses: sanjay-sridharan-personal/javascript@interview
