on:
  pull_request:
    branches: [ 'interview' ]
    types: [ opened ]

jobs:
  add_pr_label_job:
    runs-on: ubuntu-latest
    steps:
      - id: check-commit-message
        uses: sanjay-sridharan-personal/javascript@interview
